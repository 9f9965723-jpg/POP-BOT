<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Bot Dashboard</title>
    <style>
      body { font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; background: #0b1220; color: #e6edf3; margin: 0; }
      .wrap { max-width: 760px; margin: 40px auto; padding: 0 16px; }
      .card { background: #111a2e; border: 1px solid #223055; border-radius: 12px; padding: 18px; }
      h1 { margin: 0 0 12px; font-size: 22px; }
      p { margin: 6px 0 18px; color: #9fb0c0; }
      .row { display: flex; justify-content: space-between; align-items: center; padding: 12px 0; border-top: 1px solid #1f2b4a; }
      .row:first-of-type { border-top: none; }
      .label { font-weight: 600; }
      button { background: #2d6cff; color: white; border: 0; padding: 10px 14px; border-radius: 10px; cursor: pointer; font-weight: 600; }
      button:hover { background: #1f5bf0; }
      .pill { display: inline-block; font-size: 12px; padding: 4px 8px; border-radius: 999px; background: #0b1220; border: 1px solid #223055; color: #9fb0c0; }
    </style>
  </head>
  <body>
    <div class="wrap">
      <div class="card">
        <h1>Discord Bot Dashboard</h1>
        <p>Global command toggles (applies instantly).</p>

        <form method="POST" action="/settings">
          <div class="row">
            <div>
              <div class="label">/gif</div>
              <div class="pill">Media to GIF Converter</div>
            </div>
            <input type="checkbox" name="gifEnabled" <%= settings.gifEnabled ? 'checked' : '' %> />
          </div>

          <div class="row">
            <div>
              <div class="label">/guess</div>
              <div class="pill">Word Guessing Game</div>
            </div>
            <input type="checkbox" name="guessEnabled" <%= settings.guessEnabled ? 'checked' : '' %> />
          </div>

          <div class="row">
            <div>
              <div class="label">/memory</div>
              <div class="pill">Number Memory Game</div>
            </div>
            <input type="checkbox" name="memoryEnabled" <%= settings.memoryEnabled ? 'checked' : '' %> />
          </div>

          <div style="margin-top: 16px;">
            <button type="submit">Save</button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
